# My First Project in Kotlin

Este projeto é uma coleção de exemplos e exercícios desenvolvidos durante o meu aprendizado da linguagem Kotlin. Ele cobre desde os conceitos básicos até a programação orientada a objetos.

## Estrutura do Projeto

O código fonte está localizado em `src/main/kotlin` e está organizado nos seguintes módulos:

### 📂 [fiststeps](src/main/kotlin/fiststeps)
Contém o ponto de entrada inicial ("Hello World"), demonstrando a estrutura básica de um programa Kotlin.

### 📂 [variables](src/main/kotlin/variables)
Exemplos sobre declaração de variáveis (`val` vs `var`), tipos de dados primitivos (Int, Double, Boolean, Char), Strings, formatação e entrada de dados.

### 📂 [operators](src/main/kotlin/operators)
Demonstrações dos operadores da linguagem:
- Aritméticos (`+`, `-`, `*`, `/`, `%`)
- Atribuição (`=`, `+=`, `-=`)
- Lógicos (`&&`, `||`, `!`)
- Relacionais (`>`, `<`, `==`, `!=`)

### 📂 [selection](src/main/kotlin/selection)
Estruturas de controle de fluxo condicional:
- `if` e `else` (inclusive como expressão)
- `when` (a evolução do switch-case)

### 📂 [repetition](src/main/kotlin/repetition)
Estruturas de laço para iteração:
- `for`
- `while`
- `do-while`
- Controle de fluxo (`break`, `continue`, labels)

### 📂 [functions](src/main/kotlin/functions)
Estudo sobre funções em Kotlin:
- Declaração básica
- Parâmetros e argumentos nomeados
- Valores padrão
- Single-expression functions
- Funções com retorno

### 📂 [nullable](src/main/kotlin/nullable)
Conceitos de Null Safety (segurança contra nulos) do Kotlin:
- Tipos anuláveis (`String?`)
- Safe call (`?.`)
- Elvis Operator (`?:`)
- Not-null assertion (`!!`)

### 📂 [classes](src/main/kotlin/classes)
Introdução à Orientação a Objetos:
- Declaração de classes
- Construtores
- Propriedades e Métodos
- Visibilidade

### 📂 [exercises](src/main/kotlin/exercises)
Coleção de exercícios práticos aplicando os conceitos aprendidos acima.

---

## 📚 Documentação Detalhada

Para uma explicação aprofundada de cada arquivo e conceito, consulte a pasta [docs](docs/README.md).
